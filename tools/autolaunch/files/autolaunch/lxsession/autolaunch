#!/usr/bin/bash
#
# An autostart file for when system reboots and auto logs into desktop mode
# called from ~/.config/lxsession/LXDE-pi/autostart
#

if [ -d $HOME/.autolaunch/lxsession ]; then
	for I in $HOME/.autolaunch/lxsession/*.sh; do
		$I
	done
fi
